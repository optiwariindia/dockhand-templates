services:
  nats:
    image: nats:latest
    container_name: nats
    restart: unless-stopped
    command: "-js -m 8222"
    environment:
      TZ: ${TZ}
    ports:
      - "4222:4222"
      - "8222:8222"
    volumes:
      - ${DATA_PATH}:/data
