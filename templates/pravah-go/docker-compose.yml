services:
  pravah-go:
    image: optiwariindia/pravah-go:latest
    container_name: pravah-go
    restart: unless-stopped
    ports:
      - "8080:${PORT}"
    environment:
      PORT: ${PORT}
      REDIS_URL: ${REDIS_URL}
      TZ: ${TZ}
