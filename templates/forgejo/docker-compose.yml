services:
  forgejo:
    image: codeberg.org/forgejo/forgejo:9
    container_name: forgejo
    restart: unless-stopped
    environment:
      USER_UID: ${USER_UID}
      USER_GID: ${USER_GID}
      TZ: ${TZ}
    ports:
      - "3000:3000"
      - "2222:22"
    volumes:
      - ${DATA_PATH}:/data
